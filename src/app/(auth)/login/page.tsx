/**
 * Applicant login page — email input + OTP request.
 * Implemented in Phase 3 (T061).
 */
export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground">Login page — Phase 3</p>
    </div>
  );
}
